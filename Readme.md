# Mapper performace
Project aim to performace tesing database to object mapping.

dotnet ef dbcontext scaffold -o Dal "Server=.\SQLEXPRESS;Database=MapperPerformace;Trusted_Connection=True;" Microsoft.EntityFrameworkCore.SqlServer
